interface:
  display_name: "SGUA"
  short_description: "Repo-specific patterns and workflows for SGUA"
  default_prompt: "Use the SGUA repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true